name: Run Tests

on:
  push:
    branches: [main, dev-base]
  pull_request:
    branches: [main, dev-base]

jobs:
  test:
    runs-on: ubuntu-latest

    services:
      postgres:
        image: postgres:15
        env:
          POSTGRES_USER: agentic
          POSTGRES_PASSWORD: agentic
          POSTGRES_DB: agentic_db
        ports:
          - 5432:5432
        options: >-
          --health-cmd="pg_isready -U agentic"
          --health-interval=10s
          --health-timeout=5s
          --health-retries=5

    steps:
      - uses: actions/checkout@v3

      - uses: actions/setup-python@v4
        with:
          python-version: "3.12"

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt

      - name: Run tests
        env:
          TEST_DSN: postgresql://agentic:agentic@localhost:5432/agentic_db
        run: |
          pytest
